import { render } from "@testing-library/react";
import { reducer } from "./sectionReducer";
import { initialState } from "./initialState";

describe("testing reducer functions", () => {
	it("should return the type of the initial state", () => {
		// expect(reducer({}))
		reducer;
	});
	it("should be pure: not changing the current state but returning a new state");
});
